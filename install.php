<?php
/**
 * 🔴 مُحيَّد نهائياً (2026-07-30) — كان يشغّل sql/schema.sql الذي يبدأ بـ
 * "DROP DATABASE IF EXISTS saint_maxime_payroll" **بلا أي حماية** (بلا تسجيل دخول
 * ولا صلاحية ولا فحص «مركَّب مسبقاً»): أي فتحة للرابط كانت تمسح كل البيانات وتُرجع
 * كلمة مرور admin إلى الافتراضية. البرنامج مركَّب ويعمل، فلا حاجة له أبداً.
 * التركيب/التحديث يجري ذاتياً عبر migrate.php (محمي بالمدير العام) والشفاء الذاتي بالكود.
 */
http_response_code(410);
die('Gone — التركيب مُنجَز. هذه الصفحة مُلغاة نهائياً لحماية البيانات.');
